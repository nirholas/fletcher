# Archive endpoint. The official RPC and publicnode do not serve historical state and fail as fork
# sources; blockmachine does.
RHC_RPC_URL=https://rpc-robinhood.blockmachine.io

# Only needed to broadcast. Reads and simulations need nothing.
DEPLOYER_PRIVATE_KEY=
